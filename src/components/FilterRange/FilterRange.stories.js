import React from 'react';
import { storiesOf } from '@storybook/react';
import FilterRange from './FilterRange';

storiesOf('FilterRange', module)
  .add('FilterRange', () => (
    <FilterRange>
    </FilterRange>
  ));
